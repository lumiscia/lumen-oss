{
  "name": "@lumiscia/download-wasm",
  "version": "0.0.0",
  "private": true,
  "bin": {
    "download-lumen-wasm": "./dist/index.js"
  },
  "type": "module",
  "scripts": {
    "download": "vp run build && node dist/index.js",
    "check": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "devDependencies": {
    "@types/node": "^22.0.0"
  }
}
