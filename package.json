{
  "name": "lumen",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "vp check && vp run -r test && vp run -r build",
    "build:wasm": "just release",
    "build:meta:package": "just generate-node-specs",
    "build:definitions": "just generate-definitions",
    "prepare": "vp config"
  },
  "devDependencies": {
    "@types/react": "catalog:",
    "binaryen": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@10.33.0"
}
