{
  "name": "lumen",
  "version": "0.0.0",
  "private": true,
  "description": "Open-source rendering core behind Lumiscia.",
  "keywords": [
    "composition",
    "gpu",
    "lumen",
    "lumiscia",
    "media",
    "rendering"
  ],
  "homepage": "https://github.com/lumiscia/lumen-oss#readme",
  "bugs": {
    "url": "https://github.com/lumiscia/lumen-oss/issues"
  },
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lumiscia/lumen-oss.git"
  },
  "type": "module",
  "scripts": {
    "ready": "just ready",
    "changeset": "changeset",
    "version:packages": "changeset version",
    "publish:packages": "changeset publish",
    "publish:crates": "pnpm --filter @lumiscia/release-tools publish-crates",
    "sync:cargo-version": "pnpm --filter @lumiscia/release-tools sync-cargo-version",
    "release:version": "pnpm version:packages && pnpm sync:cargo-version && pnpm install --lockfile-only && cargo metadata --no-deps --format-version 1 > /dev/null",
    "release:publish": "just wasm-bindings-release && pnpm -r --filter './packages/*' --filter '!@lumiscia/lumen-bindings' build && pnpm publish:crates && pnpm publish:packages",
    "check": "pnpm format:check && pnpm lint && pnpm -r check",
    "format": "oxfmt --write .",
    "format:check": "oxfmt --check .",
    "lint": "oxlint .",
    "test": "vitest run --passWithNoTests",
    "build": "pnpm -r --filter '!lumen' --filter '!@lumiscia/lumen-bindings' build && pnpm --filter @lumiscia/lumen-bindings check"
  },
  "devDependencies": {
    "@changesets/cli": "catalog:",
    "@types/react": "catalog:",
    "binaryen": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@10.33.0"
}
