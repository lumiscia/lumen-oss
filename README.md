# Lumen TypeScript Definitions

TypeScript workspace for generated Lumen schema and metadata types.

## Packages

- `@lumen-sdk/lumen-types`

## Development

```sh
pnpm install
pnpm generate:types
pnpm check
pnpm build
```
